void main(){

  // dynamic datatype supports all data types like int, bool, string.
  //Reduced Code Readability: Code that relies heavily on dynamic types can be less readable and harder to understand because 
  //the types of variables and their expected behavior are not explicitly stated.
  // 
}
